# device_secrets.py.example
# Kopieren zu device_secrets.py und echte Werte eintragen. device_secrets.py selbst
# ist in .gitignore und wird nie committed. (Nicht "secrets.py" nennen - das
# kollidiert mit Python/Pyright's Stdlib-Modul gleichen Namens.)

WEB_USER = "admin"
WEB_PASSWORD = "change-me"
